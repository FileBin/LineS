component_dir = meson.current_source_dir()
qt_moc_headers += [
  join_paths(component_dir, 'decl.hpp')
]

qt_ui_files += [
  join_paths(component_dir, 'mainwindow.ui')
]

app_sources += [
  join_paths(component_dir, 'component.cpp')
]